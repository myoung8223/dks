defaultdelay 50   # set global default delay after each keyboard action to 50ms
delay(1000)       # wait 1 second
keys(gui, r)    # press [Windows] + [R] to open a Run dialog
string("cmd")   # type cmd
key(enter)      # press [Enter]
delay(500)                                                                                                                                          # wait a half second
string("powershell -Command "Add-Type -AssemblyName System.Speech; (New-Object System.Speech.Synthesis.SpeechSynthesizer).Speak('Hello world')"")   # type PowerShell command to say "Hello world"
key(enter)                                                                                                                                          # press [Enter]
delay(500)       # wait a half second
string("exit")   # type exit
key(enter)       # press [Enter]
led(on)      # turn on the red LED to indicate the script is done running
