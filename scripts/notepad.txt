defaultdelay 250   # 250ms delay between commands
waitblink(1)       # loop blinking LED w/2 second delay until user presses button
keys(gui, r)        # [Windows] + [R]
string("notepad")   # type "notepad"
key(enter)          # press [Enter]
delay(500)                # wait a half second for Notepad to launch
keys(control, shift, n)   # press [Ctrl] + [Shift] + [N] to open a new window
delay(1000)                                          # wait 1 second
string("This payload was deployed via Digispark!")   # type message
led(on)                                              # turn LED on indicating done